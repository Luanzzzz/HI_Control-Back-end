# ============================================
# Hi-Control Backend - Requirements (Vercel Serverless)
# ============================================
# Versão FLEXÍVEL para resolução automática do UV
# ============================================

# Core
fastapi>=0.111.0
uvicorn>=0.30.0
python-multipart

# Pydantic
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Security
bcrypt
passlib
python-jose
cryptography
pyOpenSSL

# Supabase - Deixar resolver deps transitivas (gotrue, storage3, etc)
supabase>=2.5.0

# Utils
python-dotenv
python-dateutil
typing_extensions
annotated-types

# Common
anyio
click
sniffio
h11
starlette  # Deixar o FastAPI puxar a versão correta

# XML & Validation (No C deps)
lxml==5.1.0
defusedxml
# Validation
validate-docbr
email-validator

# NF-e / PyNFE (Nota Fiscal Eletrônica)
# ⚠️ CRÍTICO para emissão e busca de notas fiscais
pynfe>=0.5.0
signxml>=3.0.0  # Requerido pelo PyNFE para assinatura digital

# HTTP Client Async (para NFS-e APIs municipais)
httpx>=0.27.0

# Email IMAP
imapclient>=3.0.0

# Google Drive / OAuth
google-api-python-client>=2.100.0
google-auth>=2.25.0
google-auth-oauthlib>=1.2.0

# Scheduler (sincronização periódica)
apscheduler>=3.10.0

# PDF Generation (DANFE, DACTE)
reportlab>=4.0.0
qrcode>=7.4.0
